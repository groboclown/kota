
import * as intern from '../../model/intern'

/*
export function attachInternPoint(moduleRelPath: string, moduleFile: string, value: intern.Internal): string {
  // The context path is the relative module file path, without the filename, + the ID.
  // If the path is 'override', then we have some special cases.

  const moduleRelPath = splitLast(moduleFile)[0]
  return joinRelativePaths(moduleRelPath, id)
  intern.isGroupDefinitionInternal
}
*/
