
// The test is just importing the root file.

import * as idx from '../index'

describe('this needs some kind of test', () => {
  it('but we dont do anything', () => {
    expect(idx.FILESTRUCTURE_VALIDATOR).not.toBeNull()
  })
})
