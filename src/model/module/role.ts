/**
 * Defines the type definition for the role in a story.  Roles are usually
 * defined just in a story, but some can be more general and define
 * a set of common characteristics.  They are also templates used to create
 * objects.
 *
 * Roles form a natural map on their own.  An object "has-a" role question
 * turns into the fit of that object's attributes to the role.
 */
