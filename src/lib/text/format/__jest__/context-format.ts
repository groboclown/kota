
import * as cf from '../context-format'

describe('FormatContext', () => {
    const formatter = new cf.FormatContext()
    describe('Given a bad format string', () => {
        describe('Then a format error message is returned', () => {
            it('', () => {

            })
        })
    })
})
